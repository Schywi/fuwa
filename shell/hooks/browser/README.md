# shell/hooks/browser

Ownership marker for shell hooks that exist to drive the browser runtime.

This is where the Wasmoon/browser preview glue belongs:

- runtime worker boot
- runtime session state
- browser deploy snapshot export
- tenant runtime coordination
