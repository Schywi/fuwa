module Home

action index(req) do
  render "home", doctype: "<!DOCTYPE html>", title: "Fuwa Lesson", summary: "This alternate payload proves the host can switch the mounted tenant without leaving the shell."
end
