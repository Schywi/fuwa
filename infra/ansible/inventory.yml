all:
  hosts:
    fuwa:
      ansible_host: "{{ lookup('env', 'FUWA_HOST') }}"
      ansible_user: root
      ansible_ssh_private_key_file: "{{ lookup('env', 'ANSIBLE_PRIVATE_KEY_FILE') | default('~/.ssh/id_ed25519', true) }}"
